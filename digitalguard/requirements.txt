# ============================================================
# DigitalGuard — Python Dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# Django Core
Django>=4.2,<5.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-environ>=0.11
django-filter>=23.5

# Database
psycopg2-binary>=2.9  # PostgreSQL driver (optional for dev, required for production)

# Security & Auth
cryptography>=41.0
PyJWT>=2.8

# AI Engine — FastAPI service
fastapi>=0.104
uvicorn[standard]>=0.24
httpx>=0.25  # async HTTP client for AI engine calls

# AI/ML
onnxruntime>=1.16
numpy>=1.24
scikit-learn>=1.3
# transformers is needed for tokenizer only (no weights sent to external APIs)
# Uncomment when you have an ONNX model:
# transformers>=4.35

# Desktop monitoring (Linux)
psutil>=5.9

# URL analysis
tldextract>=5.1
urllib3>=2.0

# Testing
pytest>=7.4
pytest-django>=4.7
pytest-asyncio>=0.21
factory-boy>=3.3
Faker>=20.1
coverage>=7.3

# FIDO2 / WebAuthn (optional — passkey support)
# py-webauthn>=2.0

# Utilities
python-decouple>=3.8
Pillow>=10.0  # image utilities
